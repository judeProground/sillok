# Git Worktree Setup

This file previously documented the `worktree.copyFiles` gitignored-file pattern table. Canonical reference now lives in the `sillok:start` skill (`skills/start/SKILL.md`, Step 10 → "copyFiles reference"). No longer `@`-imported into CLAUDE.md.
