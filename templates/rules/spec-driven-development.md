# Spec-Driven Development

This file previously documented the no-silent-drift consent gate and the spec-file-wins drift policy. The consent gate now lives in the `sillok:execute` skill (`skills/execute/SKILL.md`, Step 8); the drift policy lives in the `sillok:design` skill (`skills/design/SKILL.md`, Step 8). No longer `@`-imported into CLAUDE.md.
