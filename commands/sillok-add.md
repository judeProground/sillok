---
description: Capture a backlog issue — GH issue with Issue Type + self-assign + project status Backlog. No branch, no worktree, no milestone; pick it up later with /sillok-start <N>. Optional --parent N or owner/repo#N.
---

Invoke the `sillok:add` skill and follow it exactly. Pass the user's arguments through to the skill verbatim.
