---
description: Record a completed PRD markdown into epicRepo as <domain>/<name>/prd.md (record-only snapshot; Notion stays the source of truth). Outputs a commit-pinned permalink for /sillok-epic to link.
---

Invoke the `sillok:prd` skill and follow it exactly. Pass the user's arguments through to the skill verbatim.
