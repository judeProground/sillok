---
description: Push branch, create PR per the sillok:end skill's templates, set project status to In QA on the active sub-issue, update parent legacy checkbox if any. Does NOT auto-merge. Done note embedded in PR body Summary section.
---

Invoke the `sillok:end` skill and follow it exactly. Pass the user's arguments through to the skill verbatim.
